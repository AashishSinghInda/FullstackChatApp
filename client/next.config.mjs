/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // ya per true ko false kiya hai ye one refresh per items ko two bar print karta hai isliye ye use kiya gaya hai 
};

export default nextConfig;
